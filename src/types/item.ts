import { StaticImageData } from "next/image";

export interface Item {
  name: string;
  count: number;
}

/*export interface ItemDataType {
  id: number;
  name: string;
  img: StaticImageData;
  cost: number;
}*/

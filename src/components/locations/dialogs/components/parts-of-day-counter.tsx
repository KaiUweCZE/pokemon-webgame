// components/ui/dialog/parts-of-day-counter.tsx
export const PartsOfDayCounter = ({ current }: { current: number }) => (
  <span className="text-sm text-amber-200/60">{current}/2 parts of day used</span>
);
